services:
  frontend:
    build:
      context: .
    container_name: recipe-frontend
    env_file:
      - ./.env.production.local
    ports:
      - "3000:3000"
    restart: always
    networks:
      - recipe-network

networks:
  recipe-network:
    external: true
